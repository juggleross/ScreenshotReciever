<p id="notice"><%= notice %></p>

<h1>Screenshots</h1>

<div id="lightgallery" class="gallery-block">
  <% @screenshots.each do |screenshot| %>
    <a href="<%= screenshot.photo_url %>" >
        <img src="<%= screenshot.photo_url %>"  class = "main-screenshot" name='hello' alt=<%= screenshot.name %>/>
    </a>
  <% end %>
</div>
